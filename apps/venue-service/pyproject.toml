[project]
name = "venue-service"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "showbook-common",
    "alembic==1.13.2",
    "uvicorn[standard]==0.30.6",
    "pydantic-settings==2.4.0",
    "redis>=5.0.0",
    "httpx>=0.27.0",
]

[tool.uv.sources]
showbook-common = { workspace = true }
