[project]
name = "scraper-service"
version = "0.1.0"
description = "Scrapy spider service for movie catalog scraping"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "scrapy>=2.11.0",
    "httpx>=0.27.0",
    "pydantic-settings>=2.4.0",
    "python-dotenv>=1.0.1",
]

